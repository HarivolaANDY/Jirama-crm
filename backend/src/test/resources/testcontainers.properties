# TestContainers configuration for CI environments (GitHub Actions)
# Ensures Docker socket detection works properly
docker.host=unix:///var/run/docker.sock

# Disable Ryuk resource reaper (also set via TESTCONTAINERS_RYUK_DISABLED env var in CI)
ryuk.disabled=true
